// Temporary placeholder — LayoutWrapper coming soon
// TODO: add Navbar and shared layout shell around child pages
import { Outlet } from 'react-router-dom'

const LayoutWrapper = () => <Outlet />

export default LayoutWrapper
