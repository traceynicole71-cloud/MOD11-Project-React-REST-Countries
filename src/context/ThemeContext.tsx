// Temporary placeholder — ThemeProvider needs to be built out by teammate
// TODO: add dark/light mode state and toggle logic here
import React from 'react'

export const ThemeProvider = ({ children }: { children: React.ReactNode }) => <>{children}</>
